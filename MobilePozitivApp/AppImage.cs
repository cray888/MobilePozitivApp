using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace MobilePozitivApp
{
    class AppImage
    {
        /*mWebView.ZoomOut();
        Picture picture = mWebView.CapturePicture();
        Bitmap bitmap = Bitmap.CreateBitmap(picture.Height, picture.Width, Bitmap.Config.Argb8888);
        Canvas canvas = new Canvas(bitmap);
        picture.Draw(canvas);

        File folder = new File(Android.OS.Environment.ExternalStorageDirectory.AbsolutePath + "/Download/1C_Files/");

        if (folder.Exists() == false) folder.Mkdir();

        FileStream fos = null;
        try
        {
            fos = new FileStream(mReportAddr, FileMode.Create);
            bitmap.Compress(Bitmap.CompressFormat.Jpeg, 90, fos);
            fos.Close();
        }
        catch (Exception e)
        {
            Console.WriteLine(e);
        }*/
    }
}